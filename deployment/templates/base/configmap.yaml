apiVersion: v1
kind: ConfigMap
metadata:
  name: server-config
  namespace: default
data:
  config.yaml: |
    redis:
      addr: redis-master.redis.svc.cluster.local:6379
      password: ""
      db: 0
